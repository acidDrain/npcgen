declare namespace NpcGen {
  export interface Attributes {
    Melee: { priority: number; defaultPriority: number; value: number };
    Ranged: { priority: number; defaultPriority: number; value: number };
    Strength: { priority: number; defaultPriority: number; value: number };
    Dexterity: { priority: number; defaultPriority: number; value: number };
    Constitution: { priority: number; defaultPriority: number; value: number };
    Intelligence: { priority: number; defaultPriority: number; value: number };
    Wisdom: { priority: number; defaultPriority: number; value: number };
    Charisma: { priority: number; defaultPriority: number; value: number };
    Luck: { priority: number; defaultPriority: number; value: number };
  }
}
